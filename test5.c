int main()
{
    int a;
    float b;

    a = 10;
    c = 20; /* 1. 验证“变量未定义”错误 (c未声明) */

    int a; /* 2. 验证“变量重定义”错误 (a已存在) */

    if (a > 10   /* 3. 验证“语法错误” (缺少括号) */
        a = 5;
}

return 0;
}